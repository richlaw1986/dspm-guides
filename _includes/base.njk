<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% if title %}{{ title }} | {% endif %}DSPM Guides</title>
    <meta name="description" content="{{ description or 'Professional DSPM guides for cybersecurity experts' }}">
    <link rel="canonical" href="{{ canonical_url or ('https://www.dpsmguides.com' + page.url) }}">
    <link rel="stylesheet" href="/css/main.css">
</head>
<body>
    {% include "header.njk" %}
    
    <main>
        {{ content | safe }}
    </main>
    
    {% include "footer.njk" %}
</body>
</html>
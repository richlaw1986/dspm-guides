---
title: "Azure DSPM Guides"
description: "Comprehensive data security guides for Microsoft Azure environments"
layout: base.njk
---

<div class="container">
    
    <!-- Platform Hero Section -->
    <div class="platform-hero">
        <div class="platform-logo">AZ</div>
        <h1>Microsoft Azure Data Security</h1>
        <p>Comprehensive guides for securing your Azure environment, detecting sensitive data, and maintaining compliance across your cloud infrastructure and data services.</p>
    </div>
	
    <!-- Available Guides -->
    <div class="content-section">
        <h2 class="section-title">Azure Security Guides</h2>
        
        {# Debug: Show all available guides and their platforms #}
        <!-- Debug info (remove in production):
        {% for guide in collections.guides %}
            Platform: {{ guide.data.platform }} | Title: {{ guide.data.title }}
        {% endfor %}
        -->
        
        {# Filter guides for Azure platform #}
        {% set azureGuides = [] %}
        {% for guide in collections.guides %}
            {% if guide.data.platform == "azure" %}
                {% set azureGuides = (azureGuides.push(guide), azureGuides) %}
            {% endif %}
        {% endfor %}
        {% set guideCount = azureGuides | length %}
        
        {# Fallback: if no azure guides found, show all guides #}
        {% if guideCount == 0 %}
            {% set azureGuides = collections.guides %}
            {% set guideCount = azureGuides | length %}
        {% endif %}
        
        <p class="guides-count">{{ guideCount }} guide{{ "s" if guideCount != 1 }} available for Azure</p>
        
        <div class="guide-filters">
            <button class="filter-button active">All Guides</button>
            <button class="filter-button">Detection</button>
            <button class="filter-button">Prevention</button>
            <button class="filter-button">Compliance</button>
        </div>
        
        <div class="component-grid">
            {# Loop through Azure guides #}
            {% for guide in azureGuides %}
                {% if guide.data.status != "coming-soon" %}
                <div class="component-card" data-category="{{ guide.data.category | default('general') }}">
                    <h4><a href="{{ guide.url }}" class="guide-link-title">{{ guide.data.title }}</a></h4>
                    <p>{{ guide.data.description }}</p>
                    <div class="guide-tags-container">
                        {% if guide.data.status == 'published' %} {% if guide.data.risk %} <a href="/risk/{{ guide.data.risk | lower | replace(' ', '-') }}/" class="guide-tag risk">{{ guide.data.risk }}</a> {% endif %} {% if guide.data.regulation %} <a href="/regulation/{{ guide.data.regulation | lower | replace(' ', '-') }}/" class="guide-tag regulation">{{ guide.data.regulation }}</a> {% endif %} {% if guide.data.platform %} <a href="/platforms/{{ guide.data.platform | lower | replace(' ', '-') }}/" class="guide-tag platform">{{ guide.data.platform }}</a> {% endif %} {% else %} <span class="guide-tag coming-soon">Coming Soon</span> {% endif %}
                    </div>
                </div>
                {% endif %}
            {% endfor %}
            
            {# Coming soon guides #}
            {% for guide in azureGuides %}
                {% if guide.data.status == "coming-soon" %}
                <div class="component-card coming-soon-card">
                    <h4 class="coming-soon-title">{{ guide.data.title }}</h4>
                    <p class="coming-soon-text">{{ guide.data.description }}</p>
                    <div class="guide-tags-container">
                        <span class="guide-tag coming-soon">Coming Soon</span>
                    </div>
                </div>
                {% endif %}
            {% endfor %}
        </div>
    </div>

    <!-- What is Azure -->
    <div class="content-section">
        <h2 class="section-title">What is Microsoft Azure?</h2>
        <p>Microsoft Azure is a comprehensive cloud computing platform offering a vast collection of integrated cloud services including analytics, computing, database, mobile, networking, storage, and web capabilities. It provides enterprise-grade security, hybrid capabilities, and seamless integration with Microsoft ecosystem.</p>
        
        <div class="best-practices-grid">
            <div class="practice-card">
                <h4>Data & Analytics Services</h4>
                <ul>
                    <li>Azure Storage for scalable object storage</li>
                    <li>Azure SQL Database for managed databases</li>
                    <li>Azure Synapse Analytics for data warehousing</li>
                    <li>Azure Data Factory for data integration</li>
                </ul>
            </div>
            
            <div class="practice-card">
                <h4>AI & Machine Learning</h4>
                <ul>
                    <li>Azure Machine Learning for ML workflows</li>
                    <li>Azure Cognitive Services for AI APIs</li>
                    <li>Azure Databricks for analytics and ML</li>
                    <li>Power BI for business intelligence</li>
                </ul>
            </div>
            
            <div class="practice-card">
                <h4>Security & Governance</h4>
                <ul>
                    <li>Azure Active Directory for identity management</li>
                    <li>Azure Security Center for threat protection</li>
                    <li>Azure Policy for governance and compliance</li>
                    <li>Azure Key Vault for secrets management</li>
                </ul>
            </div>
        </div>
    </div>

    <!-- Data Security Concerns -->
    <div class="content-section">
        <h2 class="section-title">Data Security Concerns</h2>
        <p>Azure environments present unique security challenges due to their hybrid nature, extensive Active Directory integration, and diverse service portfolio. Understanding these risks is essential for maintaining robust data protection across your Microsoft cloud infrastructure.</p>
        
        <div class="concerns-grid">
            <div class="concern-card">
                <h4>Identity & Access Complexity</h4>
                <p>Complex Azure AD configurations can lead to excessive permissions and identity sprawl.</p>
                <ul>
                    <li>Over-privileged service principals</li>
                    <li>Nested Azure AD group permissions</li>
                    <li>Cross-tenant access configurations</li>
                    <li>Legacy authentication methods</li>
                </ul>
            </div>
            
            <div class="concern-card">
                <h4>Storage & Database Exposure</h4>
                <p>Misconfigured Azure storage and database services can lead to data exposure.</p>
                <ul>
                    <li>Public Azure Storage containers</li>
                    <li>Unencrypted Azure SQL databases</li>
                    <li>Cosmos DB with open firewall rules</li>
                    <li>Data Lake storage without proper access controls</li>
                </ul>
            </div>
            
            <div class="concern-card">
                <h4>Hybrid Environment Risks</h4>
                <p>Azure's hybrid capabilities can create security gaps between on-premises and cloud.</p>
                <ul>
                    <li>Inconsistent security policies across environments</li>
                    <li>Data replication without proper classification</li>
                    <li>Unsecured hybrid connections</li>
                    <li>Mixed identity governance models</li>
                </ul>
            </div>
        </div>
    </div>

    <!-- Who Are These Guides For -->
    <div class="content-section">
        <h2 class="section-title">Who Are These Guides For?</h2>
        <p class="section-description">These Azure-specific guides are designed for security professionals working with Microsoft's cloud platform and hybrid environments.</p>
        
        <div class="best-practices-grid">
            <div class="practice-card">
                <h4>Azure Security Engineers</h4>
                <ul>
                    <li>Implement Azure security best practices</li>
                    <li>Configure Microsoft Purview for data governance</li>
                    <li>Set up Azure Security Center monitoring</li>
                    <li>Manage Azure AD access controls and policies</li>
                </ul>
            </div>
            
            <div class="practice-card">
                <h4>Cloud Architects</h4>
                <ul>
                    <li>Design secure Azure landing zones</li>
                    <li>Implement encryption across Azure services</li>
                    <li>Configure network security and virtual networks</li>
                    <li>Integrate with hybrid identity solutions</li>
                </ul>
            </div>
            
            <div class="practice-card">
                <h4>Compliance & Governance Teams</h4>
                <ul>
                    <li>Ensure regulatory compliance in Azure</li>
                    <li>Audit access patterns across Azure services</li>
                    <li>Implement Azure Policy for governance</li>
                    <li>Prepare for compliance assessments and audits</li>
                </ul>
            </div>
        </div>
    </div>

    <!-- Cyera Integration -->
    <div class="cyera-highlight">
        <h3>Cyera for Microsoft Azure</h3>
        <p><a href="https://www.cyera.com">Cyera</a>'s DSPM platform provides comprehensive coverage for Azure environments, automatically discovering and classifying sensitive data across Azure Storage, SQL Database, Synapse Analytics, and other Azure services. Get real-time visibility into data risks and maintain continuous compliance with automated monitoring and alerting.</p>
    </div>

    <!-- Next Steps -->
    <div class="content-section next-steps">
        <h2 class="section-title">Get Started with Azure Security</h2>
        <p class="section-description">Begin your Azure security journey with our comprehensive guides and best practices.</p>
        <div>
            {% for guide in azureGuides | slice(0, 2) %}
                {% if guide.data.status != "coming-soon" %}
                    <a href="{{ guide.url }}" class="action-button">
                        {% if guide.data.category == "detection" %}🔍{% elif guide.data.category == "prevention" %}🛡️{% else %}📚{% endif %} 
                        {{ guide.data.title }}
                    </a>
                {% endif %}
            {% endfor %}
        </div>
    </div>
</div>

<script>
document.addEventListener('DOMContentLoaded', function() {
    const filterButtons = document.querySelectorAll('.filter-button');
    const guideCards = document.querySelectorAll('.component-card');
    
    // Define keyword mappings for each filter
    const filterKeywords = {
        'detection': ['detect', 'discover', 'scan', 'find', 'identify', 'classify'],
        'prevention': ['prevent', 'protect', 'secure', 'block', 'restrict', 'control'],
        'compliance': ['comply', 'govern', 'policy', 'regulat', 'gdpr', 'hipaa', 'sox', 'fix', 'remed']
    };
    
    filterButtons.forEach(button => {
        button.addEventListener('click', function() {
            // Remove active class from all buttons
            filterButtons.forEach(btn => btn.classList.remove('active'));
            
            // Add active class to clicked button
            this.classList.add('active');
            
            // Get the filter value
            const filterValue = this.textContent.toLowerCase().trim();
            
            // Show/hide cards based on filter
            guideCards.forEach(card => {
                if (filterValue === 'all guides') {
                    card.style.display = 'block';
                } else {
                    // Get the title text from the card
                    const titleElement = card.querySelector('h4 a.guide-link-title, h4.coming-soon-title');
                    const titleText = titleElement ? titleElement.textContent.toLowerCase() : '';
                    
                    // Get the description text from the card
                    const descElement = card.querySelector('p');
                    const descText = descElement ? descElement.textContent.toLowerCase() : '';
                    
                    // Combine title and description for searching
                    const searchText = titleText;
                    
                    // Check if any keywords match
                    const keywords = filterKeywords[filterValue] || [];
                    const hasMatch = keywords.some(keyword => searchText.includes(keyword));
                    
                    if (hasMatch) {
                        card.style.display = 'block';
                    } else {
                        card.style.display = 'none';
                    }
                }
            });
            
            // Update the guides count
            updateGuidesCount(filterValue);
        });
    });
    
    function updateGuidesCount(filterValue) {
        const visibleCards = document.querySelectorAll('.component-card[style*="block"], .component-card:not([style*="none"])');
        const countElement = document.querySelector('.guides-count');
        const visibleCount = Array.from(visibleCards).filter(card => 
            !card.style.display || card.style.display !== 'none'
        ).length;
        
        if (countElement) {
            const platform = filterValue === 'all guides' ? 'Azure' : filterValue;
            countElement.textContent = `${visibleCount} guide${visibleCount !== 1 ? 's' : ''} available for ${platform}`;
        }
    }
});
</script>